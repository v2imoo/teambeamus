{
  "name": "teambeam-in",
  "version": "1.0.0",
  "private": true,
  "description": "teambeam.in static site generator. Zero dependencies.",
  "scripts": { "build": "node build.js" },
  "engines": { "node": ">=18" }
}
